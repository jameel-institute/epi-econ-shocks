This directory is for storing figures generated using scripts in `figure-scripts/`.
Please name figures using the format `fig_xyz_abc.FMT`, where `xyz_abc` is a short description, with abbreviations allowed.
Please use a file format `.FMT` that allows maximum reuse with minimum quality loss and compression, such as `.svg`.
